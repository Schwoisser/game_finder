<%= form_for :message, url: message_index_path, method: :post do |f| %>
  Nachricht: <%= f.text_field :message %><br />
  <%= f.collection_select :receiver_id, User.all, :id, :full_name %>
  <%= f.submit %>
<% end %>