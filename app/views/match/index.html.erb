<%= link_to "Arrange Match", new_match_path %>
<% @matches.each do |match| %>
  <h2>
  <%= link_to match.title, match_path(match) %>
  </h2>
<% end %>

<% @users.each do |user| %>
  <h2>
  <%= link_to user.full_name, "user_path(user)" %>
  </h2>
<% end %>

