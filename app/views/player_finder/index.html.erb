  
  
<div class="view-width" data-controller="tab" style="">

  <div class="tab-navigation">
    <div class="tab-navigation-container">
      <div class="tab-navigation-item-active" data-tab-target="buttons" data-action="click->tab#switch" data-tab-number="0" >Players<div class="tab-navigation-corner"></div></div>
      <div class="tab-navigation-item" data-tab-target="buttons" data-action="click->tab#switch" data-tab-number="1">Games          <div class="tab-navigation-corner"></div></div>
    </div>
  </div>

  <div data-tab-target="tabs">
    <div class="bg-white py-5 px-8 rounded-b rounded-tr float-left justify-between view-width">
      <%= render :partial => 'player_finder', locals: { users: @users } %>
    </div>
  </div>

  <div class="hidden" data-tab-target="tabs">
    <div class="bg-white py-5 px-8 rounded-b rounded-tr float-left justify-between view-width">
      <%= render :partial => 'games_finder', locals: { matches: @matches } %>
    </div>
  </div>

</div>