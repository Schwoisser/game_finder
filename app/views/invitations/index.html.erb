<p id="notice"><%= notice %></p>

<h1>Invitations</h1>

<table>
  <thead>
    <tr>
      <th>Message</th>
      <th>Match</th>
      <th>Sender</th>
      <th>Reference</th>
      <th>Receiver</th>
      <th>Reference</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @invitations.each do |invitation| %>
      <tr>
        <td><%= invitation.message %></td>
        <td><%= invitation.match_id %></td>
        <td><%= invitation.sender_id %></td>
        <td><%= invitation.reference %></td>
        <td><%= invitation.receiver_id %></td>
        <td><%= invitation.reference %></td>
        <td><%= link_to 'Show', invitation %></td>
        <td><%= link_to 'Edit', edit_invitation_path(invitation) %></td>
        <td><%= link_to 'Destroy', invitation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Invitation', new_invitation_path %>
